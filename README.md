# Instructions
Create python virtual environment. Must use python 3.12.

```bash
python3.12 -m venv .venv
```

Install dependencies within venv.

```bash
source .venv/bin/activate
pip install -r requirements.txt
```

Run all notebooks within venv